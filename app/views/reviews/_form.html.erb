<%= simple_form_for [@list, @review] do |f| %>
<%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

<%= f.input :comment, label: "Comentário:" %>
<%# <%= f.input :rating, label: "Rating:", collection: 1..5 %>
<%= f.input :rating, as: :select, collection: (1..5), required: true, prompt: "Qual a nota", input_html: { id: 'review_rating' } %>

<%= f.button :submit, value: "Confirmar", class: 'btn btn-primary' %>
<% end %>
